export type DashboardStats = {
  total_complaints: number;
  open: number;
  in_progress: number;
  under_review: number;
  resolved: number;
  closed: number;
  high_priority: number;
  critical_priority: number;
};
